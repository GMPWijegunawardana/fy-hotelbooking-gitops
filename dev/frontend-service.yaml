apiVersion: v1
kind: Service
metadata:
  name: hotel-frontend-service
  namespace: dev
spec:
  selector:
    app: hotel-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort